using System;
using UnityEngine;
using System.Collections.Generic;
using TableData;

public class DummyA
{
    public List<Type> types = new List<Type>()
    {
        typeof(ItemStringData),
        typeof(string),
        typeof(float),
    };
}
